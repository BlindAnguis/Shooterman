#include "pch.h"

TEST(EngineTests, test1) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}

TEST(EngineTests, test2) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}

TEST(EngineTests, test3) {
  EXPECT_EQ(1, 1);
  EXPECT_TRUE(true);
}